[tox]
envlist = py35

[base]
deps =
    -rrequirements.txt
    -rdev-requirements.txt

[testenv]
deps =
    {[base]deps}
commands =
    coverage run -m pytest {posargs}
    coverage combine .coverage
    coverage report
